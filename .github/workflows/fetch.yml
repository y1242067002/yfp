name: Fetch

on:
  workflow_dispatch:
  schedule:
    - cron: '30 */1 * * *'
  push:
    paths:
      - 'sources.list'
      - 'config.yml'
      - 'abpwhite.txt'
      - '.github/workflows/**'
      - '**.py'
      - 'snippets/_*'

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
    # =======================
    # 1. 拉取代码
    # =======================
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # =======================
    # 2. 环境
    # =======================
    - name: Set timezone
      run: sudo timedatectl set-timezone 'Asia/Shanghai'

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'

    - name: Install dependencies
      run: pip install -r requirements.txt

    # =======================
    # 3. 执行脚本
    # =======================
    - name: Run fetch script
      run: python ./fetch.py

    # =======================
    # 4. GitHub 提交（标准稳定版）
    # =======================
    - name: Commit changes (GitHub)
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"

        git pull origin master --rebase || true

        git add list* snippets/

        git commit -m "$(date '+%Y-%m-%d %H:%M:%S') 抓取节点" || echo "no changes"

    - name: Push to GitHub
      run: |
        git push origin master --force

    # =======================
    # 5. Gitee 镜像同步（最终稳定版）
    # =======================
    - name: Sync to Gitee (mirror GitHub)
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"

        # 清理旧 remote（防报错）
        git remote remove gitee 2>/dev/null || true

        # 重新绑定 Gitee
        git remote add gitee https://${{ secrets.GITEE_USERNAME }}:${{ secrets.GITEE_TOKEN }}@gitee.com/${{ secrets.GITEE_USERNAME }}/yfp.git

        # ⭐ 核心：直接镜像 GitHub 当前分支
        git push gitee origin/master:master --force --quiet

    # =======================
    # 6. CDN 缓存清理
    # =======================
    - name: Calculate purge urls
      id: purge_urls
      run: |
        sleep 3
        baseUrl="https://cdn.jsdelivr.net/gh/${{ github.repository }}@master"
        urls=""

        for f in $(find snippets -name "*.yml" -type f); do
          urls+="${baseUrl}/${f},"
        done

        for f in $(find . -maxdepth 1 -name "list*" -type f); do
          urls+="${baseUrl}/${f},"
        done

        echo "urls=${urls%,*}" >> $GITHUB_OUTPUT

    - name: Purge JsDelivr Cache
      uses: egad13/purge-jsdelivr-cache@v1
      with:
        url: ${{ steps.purge_urls.outputs.urls }}
      continue-on-error: true
